#include "main.h"
/**
 * _abs - cmputes the absolute value of integer
 * @t: the integer to be computed
 * Return: The absolute value
 */
int _abs(int t)
{
	if (t >= 0)
		return (t);
	else
		return (-t);
}
