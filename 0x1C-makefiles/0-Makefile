# Name of the executable
EXECUTABLE = school

# Compiler
CC = gcc

# Source files
SRCS = main.c school.c

# Default rule to build the executable
all: $(EXECUTABLE)

# Rule to build the executable
$(EXECUTABLE): $(SRCS)
    $(CC) $(SRCS) -o $(EXECUTABLE)

